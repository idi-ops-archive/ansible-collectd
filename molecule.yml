---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-collectd
      image: centos/systemd
      image_version: latest  # centos/systemd doesn't provide any other tags :(
      privileged: True
      ansible_groups:
        - group1
verifier:
  name: testinfra

molecule:
  ignore_paths:
    - venv
